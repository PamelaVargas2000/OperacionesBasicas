<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    xmlns:tools="http://schemas.android.com/tools" >
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OperacionesBasicas"
        tools:targetApi="31" >
        <activity
            android:name=".DividirActivity"
            android:exported="false" />
        <activity
            android:name=".RestaActivity"
            android:exported="false" />
        <activity
            android:name=".MultiplicarActivity"
            android:exported="false" />
        <activity
            android:name=".ui.login.DividirActivity"
            android:exported="false"
            android:label="@string/title_activity_dividir" />
        <activity
            android:name=".Resta"
            android:exported="false" />
        <activity
            android:name=".SumaActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>